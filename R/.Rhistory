library(plot.pretty)
?pretty_boxplot
set.seed(1)
n <- 9
dx <- sample(1:9, size = 100, replace = TRUE, prob = c(0.2, 0.8))
dy <- stats::runif(length(dx), 0, 10)
d <- data.frame(x = dx, y = dy)
#### Example (1): Comparing graphics::boxplot() and plot.pretty::pretty_boxplot()
pp <- par(mfrow = c(1, 2))
graphics::boxplot(y ~ x, data = d)
pretty_boxplot(d$x, d$y)
par(pp)
set.seed(1)
n <- 9
dx <- sample(1:9, size = 100, replace = TRUE)
dy <- stats::runif(length(dx), 0, 10)
d <- data.frame(x = dx, y = dy)
#### Example (1): Comparing graphics::boxplot() and plot.pretty::pretty_boxplot()
pp <- par(mfrow = c(1, 2))
graphics::boxplot(y ~ x, data = d)
pretty_boxplot(d$x, d$y)
par(pp)
set.seed(1)
n <- 9
dx <- factor(sample(1:9, size = 100, replace = TRUE))
dy <- stats::runif(length(dx), 0, 10)
d <- data.frame(x = dx, y = dy)
#### Example (1): Comparing graphics::boxplot() and plot.pretty::pretty_boxplot()
pp <- par(mfrow = c(1, 2))
graphics::boxplot(y ~ x, data = d)
pretty_boxplot(d$x, d$y)
x = d$x
inherits(x, "factor")
